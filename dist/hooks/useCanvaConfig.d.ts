import { UseCanvaConfig } from "../types";
export declare const useCanvaConfig: ({ canvasRef, step, originalImageSize, renderSize, cropConfig, aspect, proportions, sizeConfig, imageUrl, confirmImage }: UseCanvaConfig) => void;
